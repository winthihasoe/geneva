import { Typography } from "@mui/material";
import React from "react";

function HelperText({ children }) {
    return <Typography fontSize={11}>{children}</Typography>;
}

export default HelperText;
